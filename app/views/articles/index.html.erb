<h1>Aricles</h1>

<ul>
    <% @articles.each do |article| %> 
    <li><a href="<%=article_path(article) %>">
        <%= article.title %>
    </a></li>
    <li><a href="<%=article_path(article) %>">
        <%= article.body %>
    </a></li>
</ul>
<% end %>